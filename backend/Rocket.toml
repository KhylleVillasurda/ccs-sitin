[default]
# 0.0.0.0 makes the backend accessible on your local network IP
# Change to 127.0.0.1 if you only want local access
address = "0.0.0.0"
port = 8000
log_level = "normal"

[default.databases.main]
url = "mysql://root:Cinetic123!@localhost:3306/ccs_sitin"

[default.limits]
json = "10MiB"
